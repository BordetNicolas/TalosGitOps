# Not rendered into gitops/ — apply with: make CLUSTER=<name> apply-doppler-token
apiVersion: v1
kind: Secret
metadata:
  name: doppler-service-token
  namespace: doppler-operator-system
type: Opaque
stringData:
  serviceToken: __DOPPLER_SERVICE_TOKEN__
